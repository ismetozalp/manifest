{
  "name": "manifest-dev",
  "private": true,
  "description": "Dev/test harness for the Manifest Cockpit plugin (not shipped).",
  "scripts": {
    "test:unit": "node --test tests/unit/*.test.js",
    "test:smoke": "node tests/smoke.mjs",
    "test:e2e": "E2E=1 node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
